apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090 # Replace with your Prometheus service name and port
    access: proxy
    isDefault: true
    editable: true
    uid: Prometheus